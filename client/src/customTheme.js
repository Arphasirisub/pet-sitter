import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: [
      "Satoshi", // Replace with your desired font family
      "sans-serif",
    ].join(","),
  },
});
export default theme;
